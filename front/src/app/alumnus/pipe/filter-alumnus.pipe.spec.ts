import { FilterAlumnusPipe } from './filter-alumnus.pipe';

describe('FilterAlumnusPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAlumnusPipe();
    expect(pipe).toBeTruthy();
  });
});
